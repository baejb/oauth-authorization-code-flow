# 기본: 텍스트 파일은 자동 정규화(저장소에는 LF)
* text=auto eol=lf

# Windows 배치 스크립트는 CRLF 유지
*.bat text eol=crlf

# Gradle wrapper 실행 스크립트는 반드시 LF (Unix 실행 가능해야 함)
gradlew text eol=lf

# 바이너리
*.jar binary
*.png binary
*.jpg binary
